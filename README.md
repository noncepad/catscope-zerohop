# Summary

`catscope-zerohop` is a CatScope’s ultra-low-latency execution layer that runs
directly alongside the validator, enabling bots and services to read state
and submit transactions without relay hops or external RPCs.
